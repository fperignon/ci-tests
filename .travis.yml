services:
  - docker

language: python

before_install:
- docker pull debian:buster
 

before_script:
  - mkdir -p build
  - cd build

script:
  - pwd
  - env
  - ls
  - uname -a
  - cat /etc/debian_version
